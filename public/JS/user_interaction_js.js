// EXERCISE 4.4.1 User Interaction. Not satisfied. I want to style the Alerts

var response = prompt("What is your name?");
	if (response == ""){
		prompt("No really, what is your name, please?");
	} else {
		alert("Welcome, " + response + "!");
	} 

// NOTE: Need way to continue until we get a name. DO WHILE LOOP or WHILE

// cancel returns null, empty field with "OK" returns empty string. 


var order = prompt ("Do you like pizza, y or n?") 
	if (order == "y") {
		alert("Woohoo!")
	} else ("bummer...")

